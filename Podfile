# Uncomment the next line to define a global platform for your project
platform :ios, '12.0'

target 'wikiPOI' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for wikiPOI
	
  pod 'RxSwift', '~> 4.0'
  pod 'RxCocoa', '~> 4.0'
  pod 'RxCoreLocation', '~> 1.3'
  pod 'RxMKMapView'
  pod 'Apollo'
  pod 'Polyline', '~> 4.0'

  target 'wikiPOITests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'wikiPOIUITests' do
    # Pods for testing
  end

end
